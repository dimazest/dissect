language: python
python:
  - "2.7"
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq python-numpy python-scipy python-matplotlib libatlas3gf-base libatlas-dev
# command to install dependencies
install: "pip install ."
# command to run tests
script: python setup.py test
